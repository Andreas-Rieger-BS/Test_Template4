openapi: 3.1.0
info:
  title: Test API
  version: 1.0.0
paths:
  /new:
    post:
      requestBody:
        description: Call 1
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/New'
          application/xml:
            schema:
              $ref: '#/components/schemas/New'  
      responses:
        '200':
          description: 200 OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/New'
            application/xml:
              schema:
                $ref: '#/components/schemas/New'
        '404':
          description: 404 not found
  /new/new:
    post:
      requestBody:
        description: Call 2
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/New'
            application/xml:
            schema:
              $ref: '#/components/schemas/New'  
      responses:
        '200':
          description: 200 OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/New'
            application/xml:
              schema:
                $ref: '#/components/schemas/New'
        '404':
          description: 404 not found

components:
  schemas:
    New:
      required:
        - id
        - name
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        tag:
          type: string
